# Blueprint: https://dashboard.render.com → New → Blueprint
# Free web service + Free Postgres (DB expires after 30 days on free).

services:
  - type: web
    name: stockpro
    runtime: docker
    plan: free
    region: singapore
    branch: main
    healthCheckPath: /login
    envVars:
      - key: APP_NAME
        value: StockPro
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: "false"
      - key: APP_LOCALE
        value: km
      - key: APP_FALLBACK_LOCALE
        value: en
      - key: APP_CURRENCY_SYMBOL
        value: $
      - key: LOG_LEVEL
        value: warning
      - key: LOG_CHANNEL
        value: stderr
      - key: SESSION_DRIVER
        value: database
      - key: CACHE_STORE
        value: database
      - key: QUEUE_CONNECTION
        value: database
      # Render's free web service filesystem is ephemeral. With IMAGE_DISK=public,
      # uploaded product pictures can disappear after a restart or redeploy.
      # For persistent pictures, set IMAGE_DISK=s3 in the Render dashboard and
      # add AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_DEFAULT_REGION,
      # AWS_BUCKET, AWS_ENDPOINT, AWS_URL, and AWS_USE_PATH_STYLE_ENDPOINT when
      # using S3-compatible storage such as Cloudflare R2.
      - key: IMAGE_DISK
        value: public
      - key: DB_CONNECTION
        value: pgsql
      - key: DB_URL
        fromDatabase:
          name: stockpro-db
          property: connectionString
      # Set these in the Render dashboard after first create:
      # APP_KEY  (php artisan key:generate --show)
      # APP_URL  (https://stockpro-xxxx.onrender.com)

databases:
  - name: stockpro-db
    plan: free
    databaseName: stockpro
    user: stockpro
    region: singapore
